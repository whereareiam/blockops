[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "blockops-publish"
version = "0.2.0"
description = "Shared release distribution tooling for BlockOps repositories"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.11.4,<3",
    "PyYAML>=6.0.1,<7",
    "requests>=2.32.3,<3",
]

[project.optional-dependencies]
test = [
    "pytest>=8.3.5,<9",
    "responses>=0.25.7,<0.26",
]

[project.scripts]
blockops-publish-distribution = "publish.cli_publish_distribution:main"
blockops-resolve-distribution-plan = "publish.cli_resolve_plan:main"
blockops-publish-publication = "publish.cli_publish_publication:main"
blockops-format-release-notes = "release.cli_format_release_notes:main"

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
